// @ts-check

/** @type {import('prettier').Config} */
const config = {
    endOfLine: 'lf',
    printWidth: 80,
    tabWidth: 4,
    semi: false,
    singleQuote: true,
    trailingComma: 'all',
    bracketSameLine: false,
    bracketSpacing: true,
}

module.exports = config
